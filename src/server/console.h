#ifndef IVY_CONSOLE_H
#define IVY_CONSOLE_H

#include <string>

namespace console{
    /**
     * Run a command and output the result to the console. Returns
     * @param command
     */
    void run_command(std::string const &command);
}
#endif //IVY_CONSOLE_H
